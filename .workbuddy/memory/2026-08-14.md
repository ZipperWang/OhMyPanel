# 2026-08-14 工作日志

## 隧道面板文案与 UI
- 删除 TunnelPanel 中三张隧道类型说明卡片（本地/远程/动态转发），保留 💡 引导文字
- tunnel.title 改为 "SSH 隧道(内网穿透) New~"，10 种语言全部本地化同步（en: Intranet Penetration, ja: イントラネット越え 等）

## 隧道错误处理（此前开发，本轮提交）
- tunnel.rs 新增 TunnelError 结构化错误码（connect_failed/prohibited/socks4/http_proxy/bad_greeting）+ SOCKS5 握手超时与协议分类
- TunnelPanel 监听 tunnel-error 事件显示本地化错误
- 提交 88f3f89 并推送 GitHub（仓库 gna1280072/LeePanel，gh 登录账号 gna1280072）

## 主题切换功能（dark/light，持久化 SQLite）
- 后端 config.rs：Settings 加 theme 字段（serde default "dark"），load/save 各一行
- App.css：全量变量化 —— :root(dark) + [data-theme=light] 两套 CSS 变量（--bg/--bg-panel/--border/--text/--accent 等），约 1000 处硬编码颜色替换为 var()，并定义了此前缺失的 --border-color
- 组件内联样式（TunnelPanel/Dashboard/MonitorPanel/DatabasePanel/FileBrowser/UpdatePanel 等）脚本批量替换为 var()
- Terminal.tsx：xterm 双主题（GitHub Dark/Light），theme prop 切换，options.theme 实时更新
- ServerSettingsPanel：App Settings 卡片加 🌙深色/☀️浅色 按钮
- App.tsx：useEffect 监听 settings.theme → <html data-theme>
- index.html 默认 data-theme="dark" 防闪烁
- i18n：settings.theme/dark/light 10 语言
- 提交 a1fe393 并推送 GitHub

## 关键经验
- Windows 沙箱会拦截 vite 清空 dist（rm 被 safe-delete 拦截），需 dangerouslyDisableSandbox 构建
- 前端变量化可用 Python 脚本批量替换（映射表驱动），但变量定义块要跳过防止 var(--bg) 循环引用
- GitHub 推送：gh auth login 后需 gh auth setup-git 接回 git 凭据
